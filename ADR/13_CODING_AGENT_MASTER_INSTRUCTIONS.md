# Coding Agent Master Instructions — Binding Full Scope

Read `14_FULL_SCOPE_BINDING_ADDENDUM.md` first, then `15_DELIVERY_ACCEPTANCE_MATRIX.md` and all original `00–12`
sections. The user requires **all** functions and **all** forks implemented in depth. Gates are dependency ordering
only; no feature, fork, UX requirement, SSO connector, store operation, icon pack, telemetry provider, Jarvis action or
original platform feature can be dropped or converted into a later suggestion.

The Seelen UI fork is **already NAI OS** and already has MCP/REST; use the actual code. Enumerate all forks under
`D:\_SATIN` and Toastovač at `D:\_SATIN_AI\Toastovac\jarvis\`. Build every fork and migrate first-party products to NAI
{Product}. Deploy NANOTRIK.AI SSO, implement native connectors, fully rebuild launcher/toolbar, install Candy Sugar as
default, deliver the 31-app JSON store, voice/captions, telemetry, media and original 00–12 commitments.

Implement all rows in the acceptance matrix and show installed/runtime evidence. The existing OpenVINO Qwen3-VL
connection is the one explicitly named next-task binding; its complete versioned API contract must be delivered now. Do
not claim the model is connected until real inference runs. Do not silently change AI providers. Preserve Windows/local
offline access during SSO, network or phone failure.
